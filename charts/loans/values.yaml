replicaCount: 1

image:
  repository: eazybank/loans
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 8090

persistence:
  enabled: true
  storageClass: ""
  accessMode: ReadWriteOnce
  size: 1Gi

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

nodeSelector: {}
tolerations: []
affinity: {}
