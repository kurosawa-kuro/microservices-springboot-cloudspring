apiVersion: v1
kind: Service
metadata:
  name: {{ include "gatewayserver.fullname" . }}
  labels:
    {{- include "gatewayserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gatewayserver.selectorLabels" . | nindent 4 }}
